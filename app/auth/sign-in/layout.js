export const metadata = {
  title: "Sign In",
  description:
    "Sign in to NexPrep AI to continue your interview preparation — AI mock interviews, instant feedback, and resume tools.",
  alternates: { canonical: "/auth/sign-in" },
};

export default function SignInLayout({ children }) {
  return children;
}
